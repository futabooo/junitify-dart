name: junitify
description: A CLI tool to convert Dart test JSON output to JUnit XML format
version: 0.1.0
homepage: https://github.com/futabooo/junitify-dart

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  args: ^2.7.0
  xml: ^6.6.1

dev_dependencies:
  test: ^1.27.0
  lints: ^6.0.0
